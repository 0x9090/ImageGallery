# Image Gallery

## Infinite scrolling image gallery
Just a simple image gallery that scrolls forever and automatically posts new images on an interval

### Installation
```
git clone https://github.com/0x9090/ImageGallery.git
cd ImageGallery && make clean build run
```